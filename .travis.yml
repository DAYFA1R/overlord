sudo: require
os: linux
language: cpp
compiler: gcc

# before_install:
#   - sudo add-apt-repository -y ppa:ubuntu-toolchain-r/test
#   - sudo apt-get update -qq

# install: 
# - sudo apt-get install g++-7 binutils-2.26 --allow-unauthenticated
# addons:
#   apt:
#     sources:
#       - ubuntu-toolchain-r-test
#     packages:
#       - gcc-7
#       - g++-7

before_install:
  - |
    if [[ "$TRAVIS_OS_NAME" == "linux" ]]; then
      sudo add-apt-repository ppa:ubuntu-toolchain-r/test -y \
      && sudo apt-get update -qq \
      && if [ "$CXX" == "g++" ]; then
        sudo apt-get install -qq g++-7 binutils-2.26 --allow-unauthenticated && export CXX="g++-7" CC="gcc-7" PATH=/usr/lib/binutils-2.26/bin:${PATH}
      fi
    fi

script:
  # Export CC and CXX to tell cmake which compiler to use
  # - export CC="gcc-7"
  # - export CXX="g++-7"
  # - export PATH=/usr/lib/binutils-2.26/bin:${PATH}
  # Check versions of gcc, g++ and cmake
  - gcc-7 -v && g++-7 -v && ld -v
  # Make app and run tests
  - make
  - make tests
  - bin/run_tests

after_success: 
  - bash <(curl -s https://codecov.io/bash)